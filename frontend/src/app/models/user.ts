export interface User {
    id: string
    code: string
    email: string
    username: string
    first_name: string
    last_name: string
    phone_number: string
    is_active: boolean
    is_verified: boolean
    is_staff: boolean
    is_deleted: boolean
    date_joined: boolean
}
