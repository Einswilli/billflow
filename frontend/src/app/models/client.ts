export interface Client {
    id: string
    code: string,
    first_name: string
    last_name: string
    address: string
    postal_code: string
    city: string
    phone: string
    email: string
}
