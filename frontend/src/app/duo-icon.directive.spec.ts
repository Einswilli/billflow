import { DuoIconDirective } from './duo-icon.directive';

describe('DuoIconDirective', () => {
  it('should create an instance', () => {
    const directive = new DuoIconDirective();
    expect(directive).toBeTruthy();
  });
});
